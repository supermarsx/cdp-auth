import { runFullCdpAuth } from './cdp-auth.js';

export default runFullCdpAuth;
export { runCdpAuth, runFullCdpAuth } from './cdp-auth.js';

/**
 * HTML encode to base64, example message
 * 
 * <p><span style='font-family: "Lucida Console", Monaco, monospace; font-size: 18px;'>Waiting for CDP connection...</span></p>
 * 
 */